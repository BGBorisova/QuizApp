<?xml version="1.0" encoding="utf-8"?>
<layout>

    <androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <ImageView
            android:id="@+id/imgInitial"
            android:padding="40dp"
            android:layout_width="@dimen/no_dimen"
            android:layout_height="@dimen/no_dimen"
            android:adjustViewBounds="true"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_initial_2"
            app:layout_constraintBottom_toTopOf="@id/txtInitialHeader"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHeight_percent="0.8"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintWidth_percent="1" />

        <TextView
            android:id="@+id/txtInitialHeader"
            android:layout_width="match_parent"
            android:layout_height="@dimen/no_dimen"
            android:layout_marginHorizontal="20dp"
            android:fontFamily="@font/dm_sans_bold"
            android:gravity="center"
            android:textAlignment="center"
            android:textColor="@color/dark_orange"
            android:textSize="22sp"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintHeight_percent="0.2"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/imgInitial" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>